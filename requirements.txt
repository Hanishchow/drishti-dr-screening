# --- core pipeline -----------------------------------------------------------
numpy>=1.24
opencv-python-headless>=4.8
scikit-image>=0.21
scikit-learn>=1.3
joblib>=1.3

# --- API ---------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1

# --- persistence -------------------------------------------------------------
SQLAlchemy>=2.0
# Postgres driver for the district deployment; SQLite needs nothing extra.
psycopg[binary]>=3.1

# --- auth --------------------------------------------------------------------
python-jose[cryptography]>=3.3
bcrypt>=4.1

# --- inference ---------------------------------------------------------------
# onnxruntime-gpu on a CUDA host; this CPU build is the portable default and is
# what the PHC edge node runs.
onnxruntime>=1.17

# --- training (not needed to serve; install on the training box) -------------
torch>=2.0
timm>=1.0
onnx>=1.15

# --- tooling -----------------------------------------------------------------
httpx>=0.27
pytest>=7.4
